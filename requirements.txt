# Requirements to run Avinash's portfolio locally
# Python 3.7+ required

# No external packages needed — the portfolio is a single HTML file.
# Just run the built-in Python HTTP server:
#
#   python -m http.server 8000
#
# Then open your browser at:
#   http://localhost:8000/avinash_portfolio.html
